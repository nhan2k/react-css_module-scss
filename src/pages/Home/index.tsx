import React, { FC } from 'react';
import Button from '~/components/Button';

interface IHome {}

const Home: FC<IHome> = () => {
  return (
    <div>
      <Button />
    </div>
  );
};

export default Home;
